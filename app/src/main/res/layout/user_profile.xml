<?xml version="1.0" encoding="utf-8"?>
<!--
    Main User Profile Screen Layout
    Modular composition of:
    - user_profile_hero.xml: Profile header with avatar, name, email, stats
    - user_profile_become_host.xml: Promotional card
    - user_profile_menu_section.xml: Settings menu items
    - dashboard_bottom_nav.xml: Bottom navigation
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5">

    <!-- Main Content ScrollView -->
    <ScrollView
        android:id="@+id/profile_scroll_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottom_nav"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Hero Section: Avatar, Name, Email, Stats -->
            <include
                layout="@layout/user_profile_hero"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

            <!-- Become a Host Card -->
            <include
                layout="@layout/user_profile_become_host"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

            <!-- Menu Items Section -->
            <include
                layout="@layout/user_profile_menu_section"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

            <!-- Bottom spacing -->
            <View
                android:layout_width="match_parent"
                android:layout_height="20dp" />
        </LinearLayout>
    </ScrollView>

    <!-- Bottom Navigation -->
    <include
        android:id="@+id/bottom_nav"
        layout="@layout/dashboard_bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>

