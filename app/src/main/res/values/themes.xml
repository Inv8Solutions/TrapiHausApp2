<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Style for all filter buttons (shared properties) -->
    <style name="ReviewFilterButton">
        <item name="android:gravity">center</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:drawablePadding">4dp</item>
    </style>

    <!-- Selected Button State (Solid Blue Background) -->
    <style name="ReviewFilterButton.Selected" parent="ReviewFilterButton">
        <item name="android:background">@drawable/review_filter_button_selected</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Unselected Button State (White Background, Blue Border) -->
    <style name="ReviewFilterButton.Unselected" parent="ReviewFilterButton">
        <item name="android:background">@drawable/reviews_filter_button_unselected</item>
        <item name="android:textColor">#007AFF</item> <!-- Primary Blue color -->
    </style>
    <style name="Theme.TrapiHausApp2" parent="android:Theme.Material.Light.NoActionBar" />
</resources>